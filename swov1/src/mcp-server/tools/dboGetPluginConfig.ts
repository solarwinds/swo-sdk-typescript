/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dboGetPluginConfig } from "../../funcs/dboGetPluginConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPluginConfigRequest$inboundSchema,
};

export const tool$dboGetPluginConfig: ToolDefinition<typeof args> = {
  name: "dbo-get-plugin-config",
  description: `Get configuration of plugins observing a database`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dboGetPluginConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
