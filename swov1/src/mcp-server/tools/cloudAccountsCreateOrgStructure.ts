/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudAccountsCreateOrgStructure } from "../../funcs/cloudAccountsCreateOrgStructure.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CloudAccountsAwsOrganisationalUnitRequest$inboundSchema,
};

export const tool$cloudAccountsCreateOrgStructure: ToolDefinition<typeof args> =
  {
    name: "cloud-accounts-create-org-structure",
    description: `Create Organizational Structure

Create AWS Organizational Structure.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await cloudAccountsCreateOrgStructure(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
