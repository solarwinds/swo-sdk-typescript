/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demSetDemSettings } from "../../funcs/demSetDemSettings.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.DemOrganizationSettingsInput$inboundSchema,
};

export const tool$demSetDemSettings: ToolDefinition<typeof args> = {
  name: "dem-set-dem-settings",
  description: `Set DEM settings`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demSetDemSettings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
