/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demUpdateUri } from "../../funcs/demUpdateUri.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateUriRequest$inboundSchema,
};

export const tool$demUpdateUri: ToolDefinition<typeof args> = {
  name: "dem-update-uri",
  description: `Update URI monitoring configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demUpdateUri(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
