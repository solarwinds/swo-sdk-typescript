/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitiesGetEntityById } from "../../funcs/entitiesGetEntityById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetEntityByIdRequest$inboundSchema,
};

export const tool$entitiesGetEntityById: ToolDefinition<typeof args> = {
  name: "entities_get-entity-by-id",
  description: `Get an entity by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await entitiesGetEntityById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
