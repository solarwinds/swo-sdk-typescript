/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demPauseWebsiteMonitoring } from "../../funcs/demPauseWebsiteMonitoring.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PauseWebsiteMonitoringRequest$inboundSchema,
};

export const tool$demPauseWebsiteMonitoring: ToolDefinition<typeof args> = {
  name: "dem-pause-website-monitoring",
  description: `Pause monitoring of a website`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demPauseWebsiteMonitoring(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
