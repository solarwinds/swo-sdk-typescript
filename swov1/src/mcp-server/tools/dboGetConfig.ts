/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dboGetConfig } from "../../funcs/dboGetConfig.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dboGetConfig: ToolDefinition = {
  name: "dbo-get-config",
  description:
    `Get organization-level configuration for database observability agents/plugins`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await dboGetConfig(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
