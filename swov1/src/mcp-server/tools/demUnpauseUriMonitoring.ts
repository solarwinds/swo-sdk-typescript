/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demUnpauseUriMonitoring } from "../../funcs/demUnpauseUriMonitoring.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UnpauseUriMonitoringRequest$inboundSchema,
};

export const tool$demUnpauseUriMonitoring: ToolDefinition<typeof args> = {
  name: "dem-unpause-uri-monitoring",
  description: `Unpause monitoring of the URI`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demUnpauseUriMonitoring(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
