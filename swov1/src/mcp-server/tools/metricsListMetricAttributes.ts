/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsListMetricAttributes } from "../../funcs/metricsListMetricAttributes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListMetricAttributesRequest$inboundSchema,
};

export const tool$metricsListMetricAttributes: ToolDefinition<typeof args> = {
  name: "metrics-list-metric-attributes",
  description: `List metric attribute names

List attribute names for a given metric.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsListMetricAttributes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
