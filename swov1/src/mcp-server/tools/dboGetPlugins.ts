/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dboGetPlugins } from "../../funcs/dboGetPlugins.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPluginsRequest$inboundSchema,
};

export const tool$dboGetPlugins: ToolDefinition<typeof args> = {
  name: "dbo-get-plugins",
  description: `Get status of plugins observing a database`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dboGetPlugins(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
