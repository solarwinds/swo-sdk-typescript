/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsListMetricMeasurements } from "../../funcs/metricsListMetricMeasurements.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListMetricMeasurementsRequest$inboundSchema,
};

export const tool$metricsListMetricMeasurements: ToolDefinition<typeof args> = {
  name: "metrics-list-metric-measurements",
  description:
    `List metric measurement values, grouped by attributes, filtered by the filter. An empty list indicates no data points are available for the given parameters.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsListMetricMeasurements(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
