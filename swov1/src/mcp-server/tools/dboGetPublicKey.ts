/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dboGetPublicKey } from "../../funcs/dboGetPublicKey.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dboGetPublicKey: ToolDefinition = {
  name: "dbo-get-public-key",
  description: `Get public key for encrypting database credentials locally`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await dboGetPublicKey(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
