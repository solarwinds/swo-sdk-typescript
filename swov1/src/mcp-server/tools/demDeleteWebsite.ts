/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demDeleteWebsite } from "../../funcs/demDeleteWebsite.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteWebsiteRequest$inboundSchema,
};

export const tool$demDeleteWebsite: ToolDefinition<typeof args> = {
  name: "dem-delete-website",
  description: `Delete website`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demDeleteWebsite(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
