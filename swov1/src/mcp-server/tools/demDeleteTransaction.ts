/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demDeleteTransaction } from "../../funcs/demDeleteTransaction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTransactionRequest$inboundSchema,
};

export const tool$demDeleteTransaction: ToolDefinition<typeof args> = {
  name: "dem-delete-transaction",
  description: `Delete transaction`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demDeleteTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
