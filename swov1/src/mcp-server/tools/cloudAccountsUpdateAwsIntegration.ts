/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudAccountsUpdateAwsIntegration } from "../../funcs/cloudAccountsUpdateAwsIntegration.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CloudAccountsAwsUpdateIntegrationRequest$inboundSchema,
};

export const tool$cloudAccountsUpdateAwsIntegration: ToolDefinition<
  typeof args
> = {
  name: "cloud-accounts-update-aws-integration",
  description: `Update AWS Integration

Update AWS Integration details.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await cloudAccountsUpdateAwsIntegration(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
