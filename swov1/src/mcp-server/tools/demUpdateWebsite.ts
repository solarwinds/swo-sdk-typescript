/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demUpdateWebsite } from "../../funcs/demUpdateWebsite.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateWebsiteRequest$inboundSchema,
};

export const tool$demUpdateWebsite: ToolDefinition<typeof args> = {
  name: "dem_update-website",
  description: `Update website monitoring configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demUpdateWebsite(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
