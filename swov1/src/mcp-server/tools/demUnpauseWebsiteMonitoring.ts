/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demUnpauseWebsiteMonitoring } from "../../funcs/demUnpauseWebsiteMonitoring.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UnpauseWebsiteMonitoringRequest$inboundSchema,
};

export const tool$demUnpauseWebsiteMonitoring: ToolDefinition<typeof args> = {
  name: "dem-unpause-website-monitoring",
  description: `Unpause monitoring of a website`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demUnpauseWebsiteMonitoring(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
