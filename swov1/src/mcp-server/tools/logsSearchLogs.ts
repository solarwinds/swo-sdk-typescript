/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { logsSearchLogs } from "../../funcs/logsSearchLogs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SearchLogsRequest$inboundSchema,
};

export const tool$logsSearchLogs: ToolDefinition<typeof args> = {
  name: "logs-search-logs",
  description: `Search logs

Search logs within a time period`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await logsSearchLogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
