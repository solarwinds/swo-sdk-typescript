/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demGetWebsite } from "../../funcs/demGetWebsite.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWebsiteRequest$inboundSchema,
};

export const tool$demGetWebsite: ToolDefinition<typeof args> = {
  name: "dem_get-website",
  description: `Get website monitoring configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demGetWebsite(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
