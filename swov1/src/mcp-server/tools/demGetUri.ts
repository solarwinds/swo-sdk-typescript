/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demGetUri } from "../../funcs/demGetUri.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUriRequest$inboundSchema,
};

export const tool$demGetUri: ToolDefinition<typeof args> = {
  name: "dem-get-uri",
  description: `Get URI monitoring configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demGetUri(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
