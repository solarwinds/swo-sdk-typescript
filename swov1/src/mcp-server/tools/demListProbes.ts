/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demListProbes } from "../../funcs/demListProbes.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$demListProbes: ToolDefinition = {
  name: "dem-list-probes",
  description: `Get a list of existing synthetic probes`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await demListProbes(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
