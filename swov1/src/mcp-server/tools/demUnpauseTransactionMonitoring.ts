/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demUnpauseTransactionMonitoring } from "../../funcs/demUnpauseTransactionMonitoring.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UnpauseTransactionMonitoringRequest$inboundSchema,
};

export const tool$demUnpauseTransactionMonitoring: ToolDefinition<typeof args> =
  {
    name: "dem-unpause-transaction-monitoring",
    description: `Unpause monitoring of the transaction`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await demUnpauseTransactionMonitoring(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
