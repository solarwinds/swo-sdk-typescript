/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dboPluginOperation } from "../../funcs/dboPluginOperation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PluginOperationRequest$inboundSchema,
};

export const tool$dboPluginOperation: ToolDefinition<typeof args> = {
  name: "dbo-plugin-operation",
  description: `Apply an operation on a database observability plugin`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dboPluginOperation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
