/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitiesListEntities } from "../../funcs/entitiesListEntities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEntitiesRequest$inboundSchema,
};

export const tool$entitiesListEntities: ToolDefinition<typeof args> = {
  name: "entities-list-entities",
  description:
    `Get a list of entities by type. A returned empty list indicates no entities matched the given parameters.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await entitiesListEntities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
